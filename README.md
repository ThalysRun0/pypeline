
```bash
python3.11 -m venv pypeline

source pypeline/bin/activate

pip install --upgrade pip

pip install pandas

pip install matplotlib

pip install networkx

pip install ipykernel

```

With jupyter kernel or interactive python session

execute every cells